import { body } from 'express-validator';
export const orderValidation = [body('customerName').isString().trim().isLength({ min: 2, max: 120 }).withMessage('Customer name is required'), body('phone').isString().trim().isLength({ min: 7, max: 30 }).withMessage('Valid phone is required'), body('address').isString().trim().isLength({ min: 5, max: 500 }).withMessage('Address is required'), body('productId').isMongoId().withMessage('Valid productId is required'), body('quantity').isInt({ min: 1 }).withMessage('Quantity must be a positive integer'), body('notes').optional().isString().trim().isLength({ max: 1000 }).withMessage('Notes are too long'), body('paymentMethod').optional().equals('Cash on Delivery').withMessage('Only Cash on Delivery is supported')];